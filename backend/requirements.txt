# MolForge AI — Backend Dependencies
# Verified compatible set as of April 2026
# Pin versions tightly — LangGraph, ChromaDB, DeepChem, RDKit all have breaking changes between minor versions

# --- Web Framework ---
fastapi==0.135.3
uvicorn[standard]==0.43.0
python-dotenv==1.0.1
httpx==0.28.1
pydantic==2.12.5
websockets==13.1

# --- LangChain / LangGraph Ecosystem ---
# Actual installed versions (April 2026) — these are 1.x releases, not 0.x
langchain==1.2.15
langgraph==1.1.6
langchain-core==1.2.27
langchain-community==0.4.1
langchain-google-genai==4.2.1

# --- Vector Store ---
# ChromaDB 0.5.x has breaking changes from 0.4.x (new client API, different collection methods)
chromadb==0.5.23

# --- Biology / Chemistry ---
biopython==1.84
# rdkit is now distributed as 'rdkit' on PyPI (not rdkit-pypi)
rdkit==2026.3.1
# DeepChem 2.8.x — pin tightly; 2.7→2.8 had TDC model loading API changes
deepchem==2.8.1
# TDC (Therapeutics Data Commons) — ADMET benchmark models
therapeutics-data-commons==0.4.1

# --- Data / Graph ---
pandas==2.2.3
networkx==3.4.2
SPARQLWrapper==2.0.0
numpy==1.26.4

# --- Report Generation ---
reportlab==4.2.5
Pillow==11.0.0

# --- Dev / Test ---
pytest==8.3.4
pytest-asyncio==0.24.0
